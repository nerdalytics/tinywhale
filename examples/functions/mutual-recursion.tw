# Mutual recursion requires forward declarations for both

is_even: (i32) -> i32
is_odd: (i32) -> i32

is_even = (n: i32): i32 ->
    match n
        0 -> 1
        _ -> is_odd(n - 1)

is_odd = (n: i32): i32 ->
    match n
        0 -> 0
        _ -> is_even(n - 1)

result:i32 = is_even(10)
