# packages/cli-compiler

CLI wrapper using AdonisJS Ace framework.

## Usage

`mise run cli <file> [options]`

- `-o <dir>` — Output directory
- `-t <format>` — Target: wasm or wat
- `--optimize` — Enable optimization
